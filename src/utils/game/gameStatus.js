export const GAME_IDLE = Symbol('__GAME_IDLE__');
export const GAME_STARTED = Symbol('__GAME_STARTED__');
export const GAME_OVER = Symbol('__GAME_OVER__');
export const GAME_PAUSED = Symbol('__GAME_PAUSED__');

export default {
  GAME_IDLE,
  GAME_STARTED,
  GAME_OVER,
  GAME_PAUSED
};
